/* Frontend public config only.
   razorpayKey = Key ID (rzp_test_ / rzp_live_)
   apiBase = backend URL. Empty = same site (Netlify Functions /api)
   Local backend: "http://localhost:8787" */
window.NP_CONFIG = {
  razorpayKey: "",
  apiBase: ""
};
